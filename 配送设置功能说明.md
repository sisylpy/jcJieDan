# 配送设置功能说明

## 功能概述

在客户详情页面新增了配送设置功能，允许用户设置客户的地理位置和配送时间范围，为后续的配送路线规划提供数据支持。

## 新增功能

### 1. 配送设置入口
- 在客户详情页面的"定价类型"下方新增"配送设置"选项
- 显示当前地理位置和配送时间的设置状态
- 点击可打开配送设置弹窗

### 2. 配送设置弹窗功能

#### 地理位置设置
- 显示当前设置的经纬度坐标
- 提供"选择位置"按钮，调用微信地图选择器
- 支持实时更新位置信息

#### 配送时间设置
- 最早配送时间选择器（TIME类型）
- 最晚配送时间选择器（TIME类型）
- 自动验证时间合理性（最晚时间不能早于最早时间）

#### 配送备注
- 文本输入框，支持输入配送相关备注信息
- 用于记录特殊配送要求

#### 配送优先级
- 下拉选择器：最高、高、中、低、最低
- 用于路线规划时的优先级排序

#### 提前配送设置
- 开关控件，控制是否允许提前配送
- 影响配送时间窗口的灵活性

## 数据库字段

### 新增字段（需要在数据库中添加）
```sql
ALTER TABLE `nx_department` 
ADD COLUMN `nx_department_delivery_notes` TEXT COMMENT '配送备注',
ADD COLUMN `nx_department_delivery_priority` TINYINT DEFAULT 3 COMMENT '配送优先级：1-最高，5-最低',
ADD COLUMN `nx_department_allow_early_delivery` TINYINT DEFAULT 1 COMMENT '是否允许提前配送：1-允许，0-不允许';
```

### 现有字段
- `nx_department_lat` - 纬度
- `nx_department_lng` - 经度  
- `nx_department_earliest_delivery_time` - 最早配送时间
- `nx_department_latest_delivery_time` - 最晚配送时间

## 使用方法

### 1. 设置地理位置
1. 点击"配送设置"
2. 在弹窗中点击"选择位置"按钮
3. 在微信地图中选择客户位置
4. 确认后位置信息会自动保存

### 2. 设置配送时间
1. 点击"最早配送时间"或"最晚配送时间"
2. 使用时间选择器设置具体时间
3. 系统会自动验证时间合理性

### 3. 其他设置
- 配送备注：直接输入文本
- 配送优先级：从下拉列表选择
- 提前配送：使用开关控制

## 技术实现

### 前端文件修改
- `customerDetail.wxml` - 新增弹窗UI
- `customerDetail.js` - 新增交互逻辑
- `customerDetail.wxss` - 新增样式

### API调用
- 使用现有的 `updateGroupName` API
- 通过 `depInfo` 对象传递所有设置信息

### 权限配置
- 已在 `app.json` 中配置 `chooseLocation` 权限
- 支持微信地图位置选择功能

## 注意事项

1. **时间验证**：系统会自动验证最早时间不能晚于最晚时间
2. **位置权限**：首次使用需要授权地理位置权限
3. **数据同步**：设置完成后会同步到本地存储和服务器
4. **错误处理**：网络错误或权限拒绝时会有相应提示

## 后续扩展

1. **路线规划**：基于地理位置和时间窗口进行智能路线规划
2. **配送优化**：根据优先级和时间窗口优化配送顺序
3. **实时追踪**：结合配送员位置进行实时配送状态更新
4. **统计分析**：基于配送数据生成配送效率分析报告 
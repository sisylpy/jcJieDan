# 货架库存初始化模板下载接口说明

## 接口概览
- **接口地址**：`GET /api/nxdistributergoodsshelfstock/downloadShelfStockTemplate`
- **接口描述**：根据指定货架，生成库存初始化 Excel 模板，便于批量录入新的库存批次。模板会包含当前货架已上架的商品基础信息，导入时只需要补充批次数量等库存数据。
- **适用场景**：在完成货架商品上架后，需要以 Excel 方式一次性录入初始库存（每条记录对应一个全新批次）。

## 请求参数
| 参数名  | 类型    | 是否必填 | 说明                           |
|---------|---------|----------|--------------------------------|
| shelfId | Integer | 是       | 货架 ID，限定模板中的商品范围 |

> 说明：缺少 `shelfId` 时，接口会直接返回错误（`RRException`），请在调用前确保前端传递正确值。

## 请求示例
```
GET /api/nxdistributergoodsshelfstock/downloadShelfStockTemplate?shelfId=1
```

## 返回结果
- **成功**：直接返回一个 `.xls` 文件，浏览器会触发下载（文件名：`货架库存初始化模板_{shelfId}.xls`）。
- **失败**：返回 JSON 结构的错误信息（例如缺少参数、生成模板异常等情况）。

### Excel 模板结构
| 列序 | 表头名称                 | 填写说明                                   | 是否预填 |
|------|--------------------------|--------------------------------------------|----------|
| A    | 货架商品ID(勿改)         | 唯一标识，请勿改动                         | 后端预填 |
| B    | 配送商商品ID             | 用于辅助识别，可保留                       | 后端预填 |
| C    | 商品名称                 | 当前货架商品名称                           | 后端预填 |
| D    | 规格                     | 规格名称                                   | 后端预填 |
| E    | 规格重量                 | 规格重量                                   | 后端预填 |
| F    | 批次剩余数量(必填)       | **必填**，新批次的剩余数量（>0）           | 需填写   |
| G    | 批次数量                 | 新批次的总数量（可与剩余数量一致）         | 选填     |
| H    | 采购单价                 | 新批次采购单价                             | 选填     |
| I    | 批次金额                 | 新批次成本金额                             | 选填     |
| J    | 批次日期(yyyy-MM-dd)     | 新批次日期                                 | 选填     |
| K    | 备注                     | 其他说明                                   | 选填     |

- 第一行提供提示信息，提醒录入 `批次剩余数量`。
- 模板中仅包含当前货架已上架的商品；若货架为空，模板只会包含表头和提示行。

## 日志输出
接口会在服务端打印以下日志，便于排查：
- `[downloadShelfStockTemplate] 请求下载模板 shelfId=...`
- `[downloadShelfStockTemplate] 货架商品数量=...`
- 若无商品：`[downloadShelfStockTemplate] 货架下暂无商品，模板仅包含表头`
- 完成输出：`[downloadShelfStockTemplate] 模板生成完成，已输出响应`
- 发生异常时：`[downloadShelfStockTemplate] 生成模板异常: ...`

## 错误码说明
| 场景                   | 返回码 | 返回信息                 | 处理建议               |
|------------------------|--------|--------------------------|------------------------|
| `shelfId` 缺失         | -1     | `"参数错误，缺少 shelfId"` | 前端补传 `shelfId`      |
| 生成或写出 Excel 失败 | -1     | `"生成模板失败"`           | 查看后台日志确认原因   |

## 使用建议
- 下载后请勿修改预填的 `货架商品ID` 列，以免后续导入失效。
- 若需要录入多个批次，可在模板中按行复制并补充数据；每行数据都会被视为新的库存批次。
- 上传导入时请保持字段顺序与表头一致，空行会自动跳过。

# 货架库存批量导入接口说明

## 接口概览
- **接口地址**：`POST /api/nxdistributergoodsshelfstock/uploadShelfStock`
- **接口描述**：读取模板 Excel，按行创建新的库存批次（每行即一个新批次），不会覆盖原有库存。
- **适用场景**：初始化库存或批量补录历史库存数据。

## 请求参数
| 参数名     | 类型      | 是否必填 | 说明                                   |
|------------|-----------|----------|----------------------------------------|
| shelfId    | Integer   | 是       | 货架 ID，应与模板中的货架一致         |
| file       | Multipart | 是       | Excel 文件，仅支持 `.xls` 模板         |
| operatorId | Integer   | 否       | 操作人 ID（用于记录批次接收人）        |

> 说明：如果 `shelfId` 缺失或文件为空，接口会直接返回错误；当模板中的货架商品 ID 与传入 `shelfId` 不符时，该行会被判定为失败。

## Excel 读取规则
- 从第 3 行（索引 2）开始读取数据；第一行为提示，第二行为表头。
- 同一行若 `货架商品ID` 与 `批次剩余数量` 均为空，会自动跳过。
- `批次剩余数量` 必填且必须大于 0；`批次数量` 为空时，默认等于剩余数量。
- 若提供 `采购单价` 和/或 `批次金额`：
  - `批次金额` 缺失时会自动用 `单价 × 批次数量` 计算。
  - `剩余金额` 优先按 `单价 × 剩余数量` 计算；若无单价，则按 `批次金额` 折算。
- `批次日期` 支持 `yyyy-MM-dd`、`yyyy/MM/dd`、`yyyy.MM.dd` 或 Excel 日期数值；为空时默认当天。

## 返回结果
```
{
  "code": 0,
  "msg": "success",
  "result": {
    "total": 10,
    "success": 7,
    "failed": 3,
    "failItems": [
      {
        "sheet": "库存初始化模板",
        "row": 5,
        "shelfGoodsId": "26861",
        "message": "批次剩余数量必须大于 0"
      }
    ]
  }
}
```

- `total`：参与导入的有效行数。
- `success`：成功写入的批次数量。
- `failed`：失败行数。
- `failItems`：失败详情（工作表、行号、货架商品 ID、失败原因）。

## 日志输出（部分）
- `[uploadShelfStock] 请求导入库存 shelfId=...`
- 成功行：`[uploadShelfStock] 导入成功 sheet=..., row=..., shelfGoodsId=..., restWeight=...`
- 失败行：`[uploadShelfStock] 行导入失败 sheet=..., row=..., 原因=...`
- 结束统计：`[uploadShelfStock] 导入完成 total=..., success=..., failed=...`

## 常见失败原因
| 场景                     | 提示信息                         | 处理建议                   |
|--------------------------|----------------------------------|----------------------------|
| 货架商品不存在           | `货架商品不存在`                 | 检查 ID 是否被手动修改     |
| 货架与模板不匹配         | `货架商品不属于当前货架`         | 确认使用的模板是否正确     |
| 剩余数量缺失或小于等于 0 | `批次剩余数量必须大于 0`         | 填写正确的剩余数量         |
| 单价/金额格式错误        | `采购单价格式错误: ...` 等提示   | 核对是否包含文本或符号     |
| 日期格式错误             | `批次日期格式不正确，应为 yyyy-MM-dd` | 使用支持的日期格式再导入 |

## 补充说明
- 导入成功后，每条记录会以正常批次（`status=0`）写入库存表，后续发货会按 FIFO 自动扣减。
- 若需要再次导入，可在模板中追加新行；重复的货架商品 ID 不会
覆盖旧批次，而是新增批次记录。
- 前端可根据 `failItems` 给出逐行提示，并允许用户修正后重新上传。

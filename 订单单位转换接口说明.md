# 订单单位转换接口说明

## 概述

本次更新对订单单位转换功能进行了重构，**不再使用单独的公斤字段（`nxDoPriceKg`、`nxDoWeightKg`）**，而是**直接修改 `nxDoPrice` 和 `nxDoWeight` 字段**。

## 接口列表

### 1. 斤转公斤接口

**接口路径：** `/api/nxdepartmentorders/phoneGetToFillDepOrdersWithKg`

**请求方式：** `POST`

**功能说明：** 将订单的单价和重量从"斤"转换为"公斤"，直接修改 `nxDoPrice` 和 `nxDoWeight` 字段。

**请求参数：**
```json
{
  "depFatherId": 123,      // 部门父ID
  "gbDepFatherId": 456,     // GB部门父ID（可选）
  "resFatherId": 789,       // 餐厅父ID（可选）
  "disId": 152              // 批发商ID
}
```

**转换规则：**
- **单价转换：** 斤单价 × 2 = 公斤单价
  - 例如：10元/斤 → 20元/公斤
- **重量转换：** 斤重量 ÷ 2 = 公斤重量
  - 例如：100斤 → 50公斤

**适用条件：**
- 商品规格（`nxDgGoodsStandardname`）包含"斤"或"市斤"
- 订单状态为 3（待处理）

**返回数据：**
```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "subAmount": 5,              // 子部门数量
    "arr": [...],                // 部门订单列表
    "tradeNo": "ABC-20241126-12345",
    "totalCount": 100,           // 总订单数
    "finishCount": 80,           // 已完成订单数
    "hasPriceCount": 90,         // 有单价订单数
    "hasWeightCount": 85,        // 有重量订单数
    "total": 50000.0,            // 总金额
    "totalHanzi": "伍万元整"      // 总金额（中文）
  }
}
```

---

### 2. 公斤转斤接口（新增）

**接口路径：** `/api/nxdepartmentorders/phoneGetToFillDepOrdersWithJin`

**请求方式：** `POST`

**功能说明：** 将订单的单价和重量从"公斤"转换为"斤"，直接修改 `nxDoPrice` 和 `nxDoWeight` 字段。

**请求参数：**
```json
{
  "depFatherId": 123,      // 部门父ID
  "gbDepFatherId": 456,     // GB部门父ID（可选）
  "resFatherId": 789,       // 餐厅父ID（可选）
  "disId": 152              // 批发商ID
}
```

**转换规则：**
- **单价转换：** 公斤单价 ÷ 2 = 斤单价
  - 例如：20元/公斤 → 10元/斤
- **重量转换：** 公斤重量 × 2 = 斤重量
  - 例如：50公斤 → 100斤

**适用条件：**
- 商品规格（`nxDgGoodsStandardname`）包含"Kg"或"kg"
- 订单状态为 3（待处理）

**返回数据：**
```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "subAmount": 5,
    "arr": [...],
    "tradeNo": "ABC-20241126-12345",
    "totalCount": 100,
    "finishCount": 80,
    "hasPriceCount": 90,
    "hasWeightCount": 85,
    "total": 50000.0,
    "totalHanzi": "伍万元整"
  }
}
```

---

## 重要变更说明

### 1. 字段使用变更

**之前：**
- 使用 `nxDoPriceKg` 和 `nxDoWeightKg` 存储转换后的公斤值
- 原值 `nxDoPrice` 和 `nxDoWeight` 保持不变

**现在：**
- **直接修改 `nxDoPrice` 和 `nxDoWeight` 字段**
- 不再使用 `nxDoPriceKg` 和 `nxDoWeightKg` 字段
- 转换后的值会**立即保存到数据库**

### 2. 数据持久化

- 转换操作会**立即更新数据库**
- 转换后的订单数据会永久保存
- **请谨慎使用，避免误操作**

### 3. 转换逻辑

#### 斤 → 公斤（`phoneGetToFillDepOrdersWithKg`）
```java
// 单价：斤单价 × 2 = 公斤单价
BigDecimal priceKg = priceDecimal.multiply(new BigDecimal("2"))
    .setScale(1, BigDecimal.ROUND_HALF_UP);

// 重量：斤重量 ÷ 2 = 公斤重量
BigDecimal weightKg = weightDecimal.divide(new BigDecimal("2"), 2, BigDecimal.ROUND_HALF_UP);
```

#### 公斤 → 斤（`phoneGetToFillDepOrdersWithJin`）
```java
// 单价：公斤单价 ÷ 2 = 斤单价
BigDecimal priceJin = priceDecimal.divide(new BigDecimal("2"), 1, BigDecimal.ROUND_HALF_UP);

// 重量：公斤重量 × 2 = 斤重量
BigDecimal weightJin = weightDecimal.multiply(new BigDecimal("2"))
    .setScale(2, BigDecimal.ROUND_HALF_UP);
```

---

## 使用示例

### 前端调用示例

#### 1. 斤转公斤
```javascript
// 微信小程序示例
wx.request({
  url: 'https://your-domain.com/api/nxdepartmentorders/phoneGetToFillDepOrdersWithKg',
  method: 'POST',
  data: {
    depFatherId: 123,
    gbDepFatherId: 456,
    resFatherId: 789,
    disId: 152
  },
  success: function(res) {
    if (res.data.code === 0) {
      console.log('转换成功', res.data.data);
    }
  }
});
```

#### 2. 公斤转斤
```javascript
// 微信小程序示例
wx.request({
  url: 'https://your-domain.com/api/nxdepartmentorders/phoneGetToFillDepOrdersWithJin',
  method: 'POST',
  data: {
    depFatherId: 123,
    gbDepFatherId: 456,
    resFatherId: 789,
    disId: 152
  },
  success: function(res) {
    if (res.data.code === 0) {
      console.log('转换成功', res.data.data);
    }
  }
});
```

---

## 注意事项

### ⚠️ 重要提醒

1. **数据不可逆**
   - 转换操作会直接修改数据库中的订单数据
   - 转换后原值会被覆盖，无法恢复
   - **建议在转换前备份数据**

2. **适用条件**
   - 只有商品规格匹配的订单才会被转换
   - 斤转公斤：商品规格必须包含"斤"
   - 公斤转斤：商品规格必须包含"Kg"或"kg"

3. **订单状态**
   - 只处理状态为 3（待处理）的订单
   - 其他状态的订单不会被转换

4. **数据验证**
   - 如果单价或重量为空，该订单不会被转换
   - 如果单价或重量格式错误，会记录日志但不会中断处理

5. **批量处理**
   - 接口会批量处理符合条件的订单
   - 处理过程中会记录详细的转换日志
   - 建议在非高峰期使用，避免影响系统性能

---

## 日志说明

转换过程中会输出详细的日志信息，包括：

- 订单ID
- 商品规格
- 原单价/重量
- 转换后单价/重量
- 错误信息（如有）

**日志示例：**
```
订单ID: 154508, 商品规格: 斤, 原单价(斤): 10.0, 转换后单价(Kg): 20.0
订单ID: 154508, 商品规格: 斤, 原重量(斤): 100.0, 转换后重量(Kg): 50.0
订单ID: 154509 已保存到数据库
```

---

## 常见问题

### Q1: 转换后如何恢复原值？
**A:** 转换操作会直接修改数据库，无法自动恢复。如需恢复，需要：
1. 从数据库备份中恢复
2. 使用反向转换接口（斤转公斤后，再用公斤转斤接口转回来）

### Q2: 为什么有些订单没有被转换？
**A:** 可能的原因：
1. 商品规格不匹配（不包含"斤"或"Kg"）
2. 订单状态不是 3
3. 单价或重量为空
4. 单价或重量格式错误

### Q3: 转换会影响订单的其他字段吗？
**A:** 不会。只修改 `nxDoPrice` 和 `nxDoWeight` 两个字段，其他字段保持不变。

### Q4: 可以只转换部分订单吗？
**A:** 可以。通过 `depFatherId`、`gbDepFatherId`、`resFatherId` 参数可以筛选需要转换的订单范围。

---

## 版本信息

- **更新日期：** 2024-11-26
- **版本：** v2.0
- **主要变更：** 移除公斤字段，直接修改原字段

---

## 联系方式

如有问题，请联系开发团队。


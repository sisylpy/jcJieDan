<view class="popup-overlay " catch:tap="stopPropagation">
 
  <view class="popup-content flex-column-center " bindtap="stopPropagation">
    <text class="popup-title ">{{popupTitle}}</text>
    
    <view class="flex-row" style="font-size: 36rpx;" >
      <text class="">{{message}}</text>
    </view>
    <view class="flex flex-column-center p-20 " style="font-size: 46rpx; width: 100%; padding: 20rpx 20rpx; margin-bottom: 20rpx; ">
    <!-- <text>{{warnContent}}</text> -->
    <radio-group class="radio-group" bindchange="radioChange" >
          <label class="radio" >
            <radio value="0" data-name="" checked="{{saveType == 0}}" />全部
          </label>
          <label class="radio" >
            <radio value="1" data-name="" checked="{{saveType == 1}}" />分部门
          </label>
        </radio-group>
    </view>

    <view class="flex  flex-row-between" style="width: 100%; padding: 40rpx;">
      <view class="cancel-button half-row" style="margin-right: 10%;margin-left: 5%;" bindtap="onCancel">取消</view>
      <view class="confirm-button half-row" bindtap="onConfirm">确定保存</view>
    </view>
  </view>
</view>

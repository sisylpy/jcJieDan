# 双单价和双零售价功能说明文档

## 📋 功能概述

为了解决外包装商品的单价核算问题，系统新增了**双单价**和**双零售价**存储功能。当商品有外包装信息时，系统会同时存储最小单位和外包装两种单位的单价，出库时根据出库单位自动选择对应的单价进行计算，避免精度误差。

## 🎯 解决的问题

### 问题场景
- **采购**：按箱采购，单价为整数（如：2箱，100元/箱）
- **入库**：转换为最小单位存储（如：48个，4.17元/个）
- **出库**：按箱出库时，如果使用最小单位单价计算，会产生精度误差

### 解决方案
- **入库时**：同时存储两种单位的单价
  - 最小单位单价：4.17元/个（用于按个出库）
  - 外包装单价：100元/箱（用于按箱出库）
- **出库时**：根据订单规格自动选择对应单价
  - 订单规格 = 外包装单位 → 使用外包装单价
  - 订单规格 ≠ 外包装单位 → 使用最小单位单价

## 📊 数据字段说明

### 数据库字段

在 `nx_distributer_goods_shelf_stock` 表中新增两个字段：

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| `nx_dgss_price` | VARCHAR(50) | 最小单位采购单价 | "4.17" |
| `nx_dgss_price_carton` | VARCHAR(50) | 外包装采购单价 | "100" |
| `nx_dgss_selling_price` | VARCHAR(50) | 最小单位建议零售价 | "5.00" |
| `nx_dgss_selling_price_carton` | VARCHAR(50) | 外包装建议零售价 | "120" |

### 商品外包装字段

在 `nx_distributer_goods` 表中：

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| `nx_dg_carton_unit` | VARCHAR(50) | 外包装单位名称 | "箱" |
| `nx_dg_items_per_carton` | INT | 每箱装数量 | 24 |

## 🔄 业务流程

### 1. 入库流程

**前提条件**：商品已设置外包装信息（`nx_dg_carton_unit` 和 `nx_dg_items_per_carton`）

**处理逻辑**：

```
采购信息：
- 采购数量：2箱
- 采购单价：100元/箱
- 建议零售价：120元/箱
- 每箱数量：24个

入库处理：
1. 数量转换：2箱 × 24 = 48个（按最小单位存储）
2. 单价计算：
   - 最小单位单价 = 100 ÷ 24 = 4.1666... ≈ 4.17元/个
   - 外包装单价 = 100元/箱（保持原始）
3. 建议零售价计算：
   - 最小单位建议零售价 = 120 ÷ 24 = 5.00元/个
   - 外包装建议零售价 = 120元/箱（保持原始）
4. 成本计算：48个 × 4.17元/个 = 200.16元
```

**代码位置**：`NxDistributerPurchaseGoodsController.purchaserReceiveBatch()`

### 2. 出库流程

**判断逻辑**：

```java
if (订单规格 == 商品外包装单位 && 库存有外包装单价) {
    使用外包装单价计算成本
    扣减数量 = 最小单位数量 ÷ 每箱数量（转换为箱数）
    成本 = 箱数 × 外包装单价
} else {
    使用最小单位单价计算成本
    成本 = 最小单位数量 × 最小单位单价
}
```

**示例**：

```
场景1：按箱出库
- 订单规格："箱"
- 商品外包装单位："箱"
- 出库数量：48个（2箱）
- 使用单价：100元/箱
- 成本计算：2箱 × 100元/箱 = 200元 ✅（无精度误差）

场景2：按个出库
- 订单规格："个"
- 商品外包装单位："箱"
- 出库数量：48个
- 使用单价：4.17元/个
- 成本计算：48个 × 4.17元/个 = 200.16元
```

**代码位置**：`NxDepartmentOrdersController` 中的出库扣减逻辑

## 📡 API 接口说明

### 1. 入库接口

**接口**：`POST /api/nxdistributerpurchasegoods/purchaserReceiveBatch`

**功能**：批量接收采购批次，自动计算并存储双单价和双零售价

**请求参数**：
```json
{
  "batchId": 123,
  "userId": 456
}
```

**处理说明**：
- 系统会自动查询商品的外包装信息
- 如果有外包装，自动计算并存储双单价和双零售价
- 如果没有外包装，按原有逻辑处理

### 2. 出库接口

**接口**：`POST /api/nxdepartmentorders/xxx`（出库相关接口）

**处理说明**：
- 系统会根据订单的 `standard` 字段判断出库单位
- 如果订单规格与商品外包装单位匹配，使用外包装单价
- 否则使用最小单位单价

## 💻 前端对接说明

### 1. 显示库存信息

在显示库存批次信息时，可以展示两种单价：

```javascript
// 库存批次对象
{
  nxDgssPrice: "4.17",              // 最小单位单价
  nxDgssPriceCarton: "100",         // 外包装单价
  nxDgssSellingPrice: "5.00",       // 最小单位建议零售价
  nxDgssSellingPriceCarton: "120", // 外包装建议零售价
  nxDgssWeight: "48",               // 库存数量（最小单位）
  // ... 其他字段
}
```

**显示建议**：
- 如果商品有外包装信息，可以同时显示两种单位的单价
- 格式：`4.17元/个` 或 `100元/箱`

### 2. 出库时单价选择

前端在创建出库订单时，系统会自动根据订单规格选择对应单价，前端无需特殊处理。

**订单对象**：
```javascript
{
  nxDoStandard: "箱",  // 订单规格
  nxDoWeight: "48",    // 出库数量（最小单位）
  // ... 其他字段
}
```

### 3. 商品外包装信息

在商品详情中，可以显示外包装信息：

```javascript
// 商品对象
{
  nxDgCartonUnit: "箱",        // 外包装单位
  nxDgItemsPerCarton: 24,      // 每箱数量
  // ... 其他字段
}
```

**显示建议**：
- 如果有外包装信息，显示：`外包装：箱，每箱24个`
- 如果没有外包装信息，不显示或显示：`无外包装`

## ⚠️ 注意事项

1. **数据库迁移**：需要先执行 SQL 脚本添加新字段
   ```sql
   -- 执行文件：添加双单价和双零售价字段.sql
   ```

2. **兼容性**：
   - 对于没有外包装信息的商品，系统按原有逻辑处理
   - 对于历史数据，如果没有外包装单价，出库时使用最小单位单价

3. **精度处理**：
   - 最小单位单价保留4位小数，显示时去除尾随零
   - 外包装单价保持整数或原始精度
   - 成本计算时使用 `BigDecimal` 确保精度

4. **数据一致性**：
   - 入库时系统自动计算并存储双单价
   - 出库时系统自动选择对应单价
   - 前端无需手动干预

## 📝 示例数据

### 示例1：有外包装的商品

**商品信息**：
- 商品名称：菌伯乐鹿茸菌
- 规格：桶
- 外包装单位：箱
- 每箱数量：24

**采购入库**：
- 采购数量：2箱
- 采购单价：100元/箱
- 建议零售价：120元/箱

**入库后存储**：
```json
{
  "nxDgssWeight": "48",              // 48个
  "nxDgssPrice": "4.1667",           // 4.1667元/个
  "nxDgssPriceCarton": "100",        // 100元/箱
  "nxDgssSellingPrice": "5",         // 5元/个
  "nxDgssSellingPriceCarton": "120", // 120元/箱
  "nxDgssSubtotal": "200.0"          // 总成本
}
```

**出库场景**：
- 按箱出库（订单规格="箱"）：使用 100元/箱，成本 = 2箱 × 100 = 200元
- 按个出库（订单规格="个"）：使用 4.1667元/个，成本 = 48个 × 4.1667 = 200.0元

### 示例2：无外包装的商品

**商品信息**：
- 商品名称：新鲜蔬菜
- 规格：kg
- 无外包装信息

**采购入库**：
- 采购数量：10kg
- 采购单价：5元/kg

**入库后存储**：
```json
{
  "nxDgssWeight": "10",
  "nxDgssPrice": "5",
  "nxDgssPriceCarton": null,        // 无外包装单价
  "nxDgssSellingPrice": "6",
  "nxDgssSellingPriceCarton": null, // 无外包装建议零售价
  "nxDgssSubtotal": "50"
}
```

**出库场景**：
- 始终使用最小单位单价：5元/kg

## 🔧 技术实现细节

### 入库逻辑

**文件**：`NxDistributerPurchaseGoodsController.java`

**关键代码**：
```java
// 检查是否有外包装信息
boolean hasCarton = disGoodsEntity != null 
    && disGoodsEntity.getNxDgCartonUnit() != null 
    && !disGoodsEntity.getNxDgCartonUnit().trim().isEmpty()
    && disGoodsEntity.getNxDgItemsPerCarton() != null 
    && disGoodsEntity.getNxDgItemsPerCarton() > 0;

if (hasCarton) {
    // 计算最小单位单价
    BigDecimal priceInMinUnit = buyPrice.divide(itemsPerCartonBD, 4, BigDecimal.ROUND_HALF_UP);
    stockEntity.setNxDgssPrice(priceInMinUnit.stripTrailingZeros().toPlainString());
    // 保存外包装单价
    stockEntity.setNxDgssPriceCarton(buyPrice.stripTrailingZeros().toPlainString());
    // ... 类似处理建议零售价
}
```

### 出库逻辑

**文件**：`NxDepartmentOrdersController.java`

**关键代码**：
```java
// 判断是否使用外包装单价
boolean useCartonPrice = false;
if (订单规格 == 商品外包装单位 && 库存有外包装单价) {
    useCartonPrice = true;
}

if (useCartonPrice) {
    // 使用外包装单价
    stockPrice = new BigDecimal(stockEntity.getNxDgssPriceCarton());
    deductWeightForCalc = deductWeight.divide(new BigDecimal(itemsPerCarton), 4, BigDecimal.ROUND_HALF_UP);
} else {
    // 使用最小单位单价
    stockPrice = new BigDecimal(stockEntity.getNxDgssPrice());
    deductWeightForCalc = deductWeight;
}
```

## 📞 联系方式

如有问题，请联系后端开发团队。

---

**文档版本**：v1.0  
**最后更新**：2024年

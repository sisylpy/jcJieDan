# 盘库接口设计文档 - 接口1：查询未盘库货架商品列表

## 1. 接口基本信息

**接口路径：**
```
POST /api/nxdistributergoodsshelfgoods/queryUnInventoriedShelfGoods
```

**接口描述：**
查询指定货架中，当前周期（日/周/月）未盘库的货架商品列表。返回的数据结构与 `getShelfGoods` 接口一致，包含货架商品信息、商品信息和未盘库的库存批次列表。

**业务场景：**
- 用户选择货架和盘库周期后，查询该货架下所有未盘库的商品
- 用于显示待盘库商品列表，支持分页
- 支持分多次完成盘库：今天盘一部分，明天继续盘

---

## 2. 请求参数

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| shelfId | Integer | 是 | 货架ID | 1 |
| inventoryType | Integer | 是 | 盘库类型：1=日盘库，2=周盘库，3=月盘库 | 2 |
| inventoryDate | String | 条件必填 | 日盘库时必填，格式：yyyy-MM-dd | "2025-01-15" |
| inventoryWeek | String | 条件必填 | 周盘库时必填，当前年第几周 | "3" |
| inventoryMonth | String | 条件必填 | 月盘库时必填，格式：MM | "01" |
| page | Integer | 否 | 页码，默认 1 | 1 |
| limit | Integer | 否 | 每页数量，默认 15 | 15 |

**参数说明：**
- `inventoryType` 为 1 时，必须提供 `inventoryDate`
- `inventoryType` 为 2 时，必须提供 `inventoryWeek`
- `inventoryType` 为 3 时，必须提供 `inventoryMonth`

---

## 3. 请求示例

### 3.1 周盘库查询
```json
{
  "shelfId": 1,
  "inventoryType": 2,
  "inventoryWeek": "3",
  "page": 1,
  "limit": 15
}
```

### 3.2 日盘库查询
```json
{
  "shelfId": 1,
  "inventoryType": 1,
  "inventoryDate": "2025-01-15",
  "page": 1,
  "limit": 15
}
```

### 3.3 月盘库查询
```json
{
  "shelfId": 1,
  "inventoryType": 3,
  "inventoryMonth": "01",
  "page": 1,
  "limit": 15
}
```

---

## 4. 返回数据结构

### 4.1 成功返回

```json
{
  "code": 0,
  "msg": "success",
  "page": {
    "totalCount": 50,        // 总记录数（货架商品数量）
    "pageSize": 15,          // 每页数量
    "totalPage": 4,          // 总页数
    "currPage": 1,           // 当前页
    "list": [                // 货架商品列表
      {
        "nxDistributerGoodsShelfGoodsId": 123,
        "nxDgsgShelfId": 1,
        "nxDgsgDisGoodsId": 456,
        "nxDgsgSort": 1,
        "nxDgsgShelfSort": 1,
        "nxDgsgShelfLayer": 1,
        "nxDistributerGoodsEntity": {
          "nxDistributerGoodsId": 456,
          "nxDgGoodsName": "苹果",
          "nxDgGoodsStandardname": "500g/袋",
          "nxDgGoodsStandardWeight": "500",
          "nxDgGoodsBrand": "品牌A",
          "nxDgGoodsPlace": "产地B",
          "nxDgGoodsDetail": "详情",
          "nxDgGoodsFile": "图片URL",
          "nxDgGoodsFileLarge": "大图URL",
          // ... 其他商品字段
        },
        "nxDisGoodsShelfStockEntities": [  // 未盘库的库存批次列表
          {
            "nxDistributerGoodsShelfStockId": 789,
            "nxDgssNxDistributerId": 1,
            "nxDgssNxDisGoodsId": 456,
            "nxDgssNxDisGoodsFatherId": 100,
            "nxDgssWeight": "100.0",           // 批次数量
            "nxDgssRestWeight": "50.0",        // 剩余数量（账面）
            "nxDgssPrice": "25.0",             // 单价
            "nxDgssRestSubtotal": "1250.0",    // 剩余成本
            "nxDgssDate": "2025-01-10",        // 批次日期
            "nxDgssStatus": 0,                 // 批次状态：0=正常
            "nxDgssNxShelfGoodsId": 123,       // 货架商品ID
            "nxDgssNxPurGoodsId": 999,         // 采购商品ID
            // ... 其他批次字段
          }
        ]
      }
    ]
  }
}
```

### 4.2 错误返回

```json
{
  "code": 500,
  "msg": "错误信息"
}
```

**常见错误：**
- `"货架ID不能为空"`
- `"盘库类型无效，必须是 1(日)、2(周) 或 3(月)"`
- `"日盘库必须提供盘库日期"`
- `"周盘库必须提供盘库周数"`
- `"月盘库必须提供盘库月份"`

---

## 5. 业务逻辑

### 5.1 查询条件

1. **货架商品筛选：**
   - `nx_DGSG_shelf_id = #{shelfId}`

2. **库存批次筛选（未盘库）：**
   - `nx_dgss_rest_weight > 0`（有剩余库存）
   - `nx_dgss_status = 0`（正常状态）
   - `nx_dgss_nx_shelf_goods_id` 对应货架商品
   - **当前周期没有 `type=2` 的盘库记录**（核心条件）

3. **盘库记录判断：**
   - 日盘库：`nx_dgssr_date = #{inventoryDate}` AND `nx_dgssr_type = 2`
   - 周盘库：`nx_dgssr_week = #{inventoryWeek}` AND `nx_dgssr_type = 2`
   - 月盘库：`nx_dgssr_month = #{inventoryMonth}` AND `nx_dgssr_type = 2`

### 5.2 查询逻辑

```sql
-- 伪代码逻辑
SELECT 货架商品信息, 商品信息, 未盘库的批次列表
FROM nx_distributer_goods_shelf_goods (货架商品表)
LEFT JOIN nx_distributer_goods (商品表)
LEFT JOIN nx_distributer_goods_shelf_stock (库存批次表)
LEFT JOIN nx_distributer_goods_shelf_stock_reduce (盘库记录表)
  ON 批次ID = 盘库记录.批次ID
  AND 盘库记录.type = 2
  AND 当前周期条件匹配
WHERE 
  货架ID = #{shelfId}
  AND 批次.剩余重量 > 0
  AND 批次.状态 = 0
  AND 盘库记录.ID IS NULL  -- 当前周期未盘库
ORDER BY 货架商品排序 ASC
```

### 5.3 排序规则

- 货架商品按 `nx_DGSG_sort` 升序排列
- 批次按 `nx_dgss_date` 升序排列（FIFO：先盘旧批次）

---

## 6. 实现要点

### 6.1 DAO 层

**文件：** `NxDistributerGoodsShelfGoodsDao.xml`

**方法名：** `queryUnInventoriedShelfGoods`

**ResultMap：** 使用现有的 `shelfGoods` resultMap

**关键点：**
- 使用 `LEFT JOIN` 关联盘库记录表
- 在 JOIN 条件中加入周期判断
- WHERE 条件中判断 `盘库记录ID IS NULL`

### 6.2 Service 层

**文件：** `NxDistributerGoodsShelfGoodsService.java`

**方法签名：**
```java
List<NxDistributerGoodsShelfGoodsEntity> queryUnInventoriedShelfGoods(Map<String, Object> map);
Integer queryUnInventoriedShelfGoodsCount(Map<String, Object> map);
```

### 6.3 Controller 层

**文件：** `NxDistributerGoodsShelfGoodsController.java`

**方法签名：**
```java
@RequestMapping(value = "/queryUnInventoriedShelfGoods", method = RequestMethod.POST)
@ResponseBody
public R queryUnInventoriedShelfGoods(
    Integer shelfId,
    Integer inventoryType,
    String inventoryDate,
    String inventoryWeek,
    String inventoryMonth,
    Integer page,
    Integer limit
)
```

**参数校验：**
1. `shelfId` 不能为空
2. `inventoryType` 必须是 1、2 或 3
3. 根据 `inventoryType` 校验对应的周期参数

**分页处理：**
- 默认值：`page = 1`, `limit = 15`
- 计算 `offset = (page - 1) * limit`

**返回格式：**
- 使用 `PageUtils` 包装分页结果
- 返回格式：`R.ok().put("page", pageUtil)`

---

## 7. 边界情况处理

### 7.1 空结果
- 如果货架下没有商品，返回空列表
- 如果所有商品都已盘库，返回空列表
- 如果商品有库存但没有未盘库的批次，该商品仍会返回，但 `nxDisGoodsShelfStockEntities` 为空数组

### 7.2 数据一致性
- 如果批次状态不是 0，不显示
- 如果批次剩余重量 <= 0，不显示
- 如果当前周期已有盘库记录（`type=2`），该批次不显示

### 7.3 周期判断
- **日盘库：** 精确匹配日期
- **周盘库：** 匹配周数（格式：当前年第几周，如 "3"）
- **月盘库：** 匹配月份（格式：MM，如 "01"）

---

## 8. 测试用例

### 8.1 正常场景
- 查询货架ID=1，周盘库，周数=3，返回未盘库的商品列表

### 8.2 边界场景
- 货架下没有商品
- 所有商品都已盘库
- 商品有库存但所有批次都已盘库

### 8.3 异常场景
- `shelfId` 为空
- `inventoryType` 无效
- 周盘库但未提供 `inventoryWeek`
- 日盘库但未提供 `inventoryDate`

---

## 9. 注意事项

1. **性能优化：**
   - 确保 `nx_distributer_goods_shelf_stock_reduce` 表有索引：
     - `(nx_dgssr_nx_stock_id, nx_dgssr_type, nx_dgssr_week)`
     - `(nx_dgssr_nx_stock_id, nx_dgssr_type, nx_dgssr_month)`
     - `(nx_dgssr_nx_stock_id, nx_dgssr_type, nx_dgssr_date)`

2. **数据准确性：**
   - 确保盘库记录表的 `type=2` 只用于盘库损失记录
   - 确保周期字段（`week`/`month`/`date`）格式一致

3. **扩展性：**
   - 如果未来需要支持其他盘库类型，可以扩展 `inventoryType` 枚举

---

## 10. 相关接口

- **查询已盘库商品：** `queryInventoriedShelfGoods`（接口2）
- **查询货架商品：** `getShelfGoods`（现有接口，参考数据结构）
- **保存盘库记录：** `saveInventoryRecord`（接口4）

---

## 11. 更新日志

- **2025-01-XX：** 初始版本，支持日/周/月盘库查询未盘库商品列表


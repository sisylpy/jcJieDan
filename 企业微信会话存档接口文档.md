# 企业微信会话存档接口文档

## 📋 接口概览

本文档提供了企业微信会话存档功能的完整接口说明，包括获取群消息、解密消息内容等功能。

## 🔑 基础信息

- **基础URL**: `https://grainservice.club/api/qywx`
- **企业ID**: `ww9778dea409045fe6`
- **回调URL**: `https://grainservice.club:8443/nongxinle/api/qywx/msgaudit/callback`

## 📡 接口列表

### 1. 测试会话存档功能

**接口地址**: `GET /customergroup/test-msgaudit`

**请求参数**:
| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| corpId | String | 否 | ww9778dea409045fe6 | 企业微信企业ID |

**请求示例**:
```bash
GET /api/qywx/customergroup/test-msgaudit?corpId=ww9778dea409045fe6
```

**响应示例**:
```json
{
    "code": 0,
    "data": {
        "corpId": "ww9778dea409045fe6",
        "message": "会话存档功能测试",
        "status": "ready",
        "configStatus": "已配置",
        "secretConfigured": true,
        "privateKeyConfigured": true,
        "instructions": "请在外部客户群中发送消息来测试会话存档功能",
        "callbackUrl": "https://grainservice.club:8443/nongxinle/api/qywx/msgaudit/callback",
        "timestamp": 1760815438119
    }
}
```

### 2. 检查会话存档配置状态

**接口地址**: `GET /customergroup/secret-test`

**请求参数**:
| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| corpId | String | 否 | ww9778dea409045fe6 | 企业微信企业ID |

**请求示例**:
```bash
GET /api/qywx/customergroup/secret-test?corpId=ww9778dea409045fe6
```

**响应示例**:
```json
{
    "code": 0,
    "msg": "success",
    "data": {
        "corpId": "ww9778dea409045fe6",
        "message": "配置检查完成",
        "configExists": true,
        "secret": "QYb4xvJnu47bCClSC5PgOqJQrpM4iRy_BJv0BpEiBZ4",
        "secretConfigured": true,
        "secretLength": 43,
        "token": "Sg3d1Bp1tBkvEJeT0A3F9H4lFWNWzY",
        "encodingAesKey": "pFkjhhUR8IVMkfWLs6VftVgbSe1U6GsVH1lWqAR5aTc",
        "privateKeyConfigured": true,
        "status": 1,
        "timestamp": 1697651234567,
        "statusMessage": "Secret配置正确，可以开始使用会话存档功能"
    }
}
```

### 2. 获取已发现的群聊列表

**接口地址**: `GET /customergroup/discovered-groups`

**请求参数**:
| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| corpId | String | 否 | ww9778dea409045fe6 | 企业微信企业ID |

**请求示例**:
```bash
GET /api/qywx/customergroup/discovered-groups?corpId=ww9778dea409045fe6
```

**响应示例**:
```json
{
    "code": 0,
    "data": {
        "corpId": "ww9778dea409045fe6",
        "message": "已发现的群聊列表",
        "discoveredGroups": [],
        "note": "群聊信息会在收到消息时自动发现并存储",
        "timestamp": 1760815438119
    }
}
```

### 3. 获取客户群列表

**接口地址**: `GET /customergroup/list`

**说明**: 由于使用会话存档功能，此接口返回空列表。群聊信息通过消息推送自动发现。

**请求参数**:
| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| corpId | String | 否 | ww9778dea409045fe6 | 企业微信企业ID |
| limit | Integer | 否 | 100 | 返回数量限制 |
| cursor | String | 否 | "" | 游标，用于分页 |

**请求示例**:
```bash
GET /api/qywx/customergroup/list?corpId=ww9778dea409045fe6&limit=50
```

**响应示例**:
```json
{
    "total": 0,
    "code": 0,
    "list": []
}
```

**说明**: 
- 如果返回空列表，说明当前没有外部客户群
- 如果有群数据，会返回完整的群信息结构

### 3. 获取群成员详情

**接口地址**: `GET /customergroup/member`

**请求参数**:
| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| corpId | String | 是 | - | 企业微信企业ID |
| chatId | String | 是 | - | 群聊ID |

**请求示例**:
```bash
GET /api/qywx/customergroup/member?corpId=ww9778dea409045fe6&chatId=wrOgQhDgAAMYQiS5ol9G7gK9JVAAAA
```

**响应示例**:
```json
{
    "code": 0,
    "msg": "success",
    "data": {
        "chat_id": "wrOgQhDgAAMYQiS5ol9G7gK9JVAAAA",
        "member_list": [
            {
                "userid": "zhangsan",
                "type": "normal",
                "join_time": 1234567890,
                "name": "张三",
                "unionid": "o6_bmjrPTlm6_2sgVt7hMZOPfL2M"
            }
        ]
    }
}
```

### 4. 手动处理消息（测试用）

**接口地址**: `POST /msgaudit/process`

**请求参数**:
| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| corpId | String | 是 | - | 企业微信企业ID |
| msgId | String | 是 | - | 消息ID |

**请求示例**:
```bash
POST /api/qywx/msgaudit/process
Content-Type: application/x-www-form-urlencoded

corpId=ww9778dea409045fe6&msgId=123456789
```

**响应示例**:
```json
{
    "code": 0,
    "msg": "success",
    "data": {
        "message": "消息处理完成",
        "msgId": "123456789",
        "processed": true
    }
}
```

### 5. 获取spring配置信息

**接口地址**: `GET /customergroup/spring-config`

**请求参数**:
| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| corpId | String | 否 | ww9778dea409045fe6 | 企业微信企业ID |

**请求示例**:
```bash
GET /api/qywx/customergroup/spring-config?corpId=ww9778dea409045fe6
```

**响应示例**:
```json
{
    "code": 0,
    "msg": "success",
    "data": {
        "corpId": "ww9778dea409045fe6",
        "token": "Sg3d1Bp1tBkvEJeT0A3F9H4lFWNWzY",
        "secretConfigured": true,
        "privateKeyConfigured": true,
        "status": 1
    }
}
```

## 🔄 消息处理流程

### 自动接收消息流程

1. **企业微信推送消息** → 回调接口 `/msgaudit/callback`
2. **系统自动解密消息** → 获取msgId
3. **调用会话存档API** → 获取完整消息内容
4. **RSA解密消息内容** → 获得可读文本
5. **业务逻辑处理** → 自动回复等操作

### 消息数据结构

```json
{
    "msgid": "消息ID",
    "action": "send|recall",
    "from": "发送者ID",
    "roomid": "群聊ID",
    "msgtime": 1234567890,
    "msgtype": "text|image|voice|video|file",
    "content": "加密的消息内容（需要RSA解密）"
}
```

## 🚨 错误码说明

| 错误码 | 说明 | 解决方案 |
|--------|------|----------|
| 0 | 成功 | - |
| -1 | 系统错误 | 检查服务器状态 |
| 40013 | 不合法的corpid | 检查企业ID是否正确 |
| 40001 | 不合法的secret | 检查Secret配置 |
| 40014 | 不合法的access_token | 检查Token是否过期 |
| 85005 | 企业未开通会话存档 | 联系企业微信开通功能 |

## 🔧 测试建议

### 1. 配置检查
```bash
# 检查配置是否完整
GET /api/qywx/customergroup/secret-test
```

### 2. 群列表测试
```bash
# 获取客户群列表
GET /api/qywx/customergroup/list?limit=10
```

### 3. 消息接收测试
在企业微信外部群中发送一条消息，观察服务器日志：
- 查看是否有消息推送日志
- 检查消息解密是否成功
- 验证自动回复功能

## 📝 注意事项

1. **权限要求**: 需要企业微信会话存档功能权限（付费功能）
2. **回调验证**: 首次配置需要验证回调URL
3. **消息加密**: 所有消息内容都经过RSA加密，需要私钥解密
4. **频率限制**: 企业微信API有调用频率限制
5. **合规要求**: 建议在群公告中说明消息会被记录

## 🔗 相关文档

- [企业微信会话存档API文档](https://developer.work.weixin.qq.com/document/path/91774)
- [RSA加密解密说明](https://developer.work.weixin.qq.com/document/path/91782)
- [消息格式说明](https://developer.work.weixin.qq.com/document/path/91784)

---

**文档版本**: v1.0  
**更新时间**: 2024-10-19  
**维护人员**: lpy

# AGENTS 开发规则（农心乐 Boss 小程序）

本仓库正式主线为 `main`，开发在正式目录直接进行、提交。

## 默认规则
- 在正式目录 `main` 直接修改、提交；未经用户明确要求，不新建分支、worktree 或隔离副本。
- 同一仓库同时只允许一个 Agent 写入。
- 只读取相关代码与必要文档，不每次扫描所有历史资料。
- 普通修改做必要定向检查，不默认全量测试、构建或运行 Runner。
- 不因“保存版本”自动打开开发者工具、生成预览、上传体验版、提交审核或发布小程序。
- 本次有效改动及其依赖一起提交；推送按授权执行。
- 提交、推送、真机验收和发布是不同状态，分别说明。
- 不通过删除测试、忽略错误或隐藏改动来制造“完成”。

## 业务与配置
- 不擅自修改 AppID、接口地址、环境开关或后端接口合同。
- 区分正式项目配置与本机私有配置；不向 Git 新增密钥、令牌、缓存、日志或构建产物。
- 保留并随页面提交其依赖的图标/图片等静态资源，不无差别清理。
